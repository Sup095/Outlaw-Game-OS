# ============================================================================
# Outlaw CodeMaker — Linux runtime requirements.
# ----------------------------------------------------------------------------
# Used by outlaw-install on the OS to seed the venv at
# /opt/outlaw-codemaker/.venv. Mirrors requirements.txt minus pywin32 (which
# is Windows-only and would fail to install on Arch).
#
# Note on pygetwindow: kept in this list for compatibility with the Windows
# backend's lazy import path. On Linux, vision/godot_bridge.py uses an
# xdotool-subprocess backend (no Python deps required); pygetwindow installs
# but is never imported.
#
# System packages installed alongside (via pacman, not pip):
#   * xdotool          — the godot_bridge Linux backend AND pyautogui input
#   * scrot            — pyautogui's Linux screenshot backend
#   * tesseract, tesseract-data-eng  — pytesseract's OCR engine
# ============================================================================

PyQt6>=6.7.0
openai>=1.40.0
opencv-python>=4.10.0
pyautogui>=0.9.54
pytesseract>=0.3.10
pygetwindow>=0.0.9
pygments>=2.17.0
sqlitedict>=2.1.0
mss>=9.0.1
Pillow>=10.4.0
numpy>=1.26.0
psutil>=5.9.0
nvidia-ml-py>=12.535.0
pydantic>=2.8.0
httpx>=0.27.0
python-dotenv>=1.0.1
